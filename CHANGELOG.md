# 更新日志

本项目的所有显著变更均记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本（SemVer）](https://semver.org/lang/zh-CN/)。

---

## [0.1.4] · 2026-08-17

### 新增

- 上传与下载弹窗均支持远程目录列表、双击进入子目录、返回上级目录及手动输入路径读取。
- 下载列表支持勾选多个文件；选择一次本地目标文件夹后即可统一保存。
- 上传弹窗支持一次选择多个本地文件，并将其批量上传至当前远程目录。

### 优化

- 统一上传、下载弹窗的目录栏、底部操作栏及浅色模式配色。
- 目录读取加入请求与会话隔离，避免切换目录、会话或关闭弹窗后出现过期列表覆盖。
- 强化上传数据完整性校验，阻止超额或不完整数据被误标记为上传成功。
- 下载前校验远程目标为普通文件，避免目录或特殊文件导致传输异常。

### 修复

- 补齐 Escape 对上传、下载及上传冲突弹窗的关闭处理。
- 移除已废弃的上传目录验证协议及前端状态逻辑，降低维护复杂度。

---

## [0.1.3] · 2026-08-17

### 新增

- 统一浏览器 favicon、Apple Touch Icon、终端空状态入口、fnOS 应用及介绍站点的图标资源。
- 已保存会话列表改为显示会话名称与“主机:端口 · 用户名”双行信息，便于快速识别。
- 上传目录验证通过后，操作按钮会切换为“选择文件”，清晰展示当前状态。
- 更新 README 与更新日志的版式，并优化产品介绍站点的导航、主题切换与视觉效果。
- 已保存的 SSH 连接配置改为 AES-256-GCM 加密存储，支持旧版明文配置自动迁移。
- 支持独立密钥文件或外部托管密钥；配置读写采用原子替换和串行化处理。
- WebSocket 增加可信来源校验，降低跨站调用本地 SSH 服务的风险。

### 优化

- 修复 Docker 与 fnOS 环境下自定义背景、背景图片及用户偏好的持久化，并统一保存至数据卷或应用数据目录。
- 修正 fnOS 安装包维护者与分发者链接为正式项目仓库地址。
- 完善敏感 SSH 连接配置的 Git 忽略规则，避免本地凭据被误提交。
- 用户偏好设置从浏览器 `localStorage` 迁移至服务端 `settings.json`，降低清理浏览器缓存导致设置丢失的风险。
- 优化暗色模式会话拖拽配色、保存连接区域高度、固定/删除按钮尺寸与跨浏览器兼容性。
- 移除私钥口令输入框，同时保留旧数据兼容。
- 为资源引用增加版本查询参数，减少 Safari 的模块缓存问题。
- 统一 0.1.2 的应用、Docker、Windows 与 fnOS 发布元数据，并清理配置规范化中的重复逻辑。
- Windows 便携版、Docker 与 fnOS 包不再初始化明文连接配置。

### 修复

- 修复新版 xterm.js 中终端字体设置调用旧 API 导致的错误。
- 修复 Safari 中目录验证后无法打开文件选择框，以及上传时可能出现空对象访问错误的问题。
- 将 xterm.js、addon-fit 与样式本地化；修复 `ResizeObserver` 循环及相关 Safari 页面错误提示。
- 重写会话标签拖拽为 Pointer Events 自绘实现，修复触摸设备支持、边界溢出、Safari ghost 渲染和多标签误选中问题。
- 修复常驻会话被设置后自动建立 SSH 连接的问题。
- 修复上传冲突弹窗的页面闪烁与快速连续触发时的竞态条件。

---

## [0.1.1] · 2026-08-14

### 新增

- 终端字体设置新增字体颜色调整，修改后实时生效并持久化保存。
- 美化背景设置弹窗，并增加背景图片状态提示。
- 服务端新增 SPA 回退路由，未知页面路径将返回 `index.html`，API 路由不受影响。

### 优化

- 合并重复的 Escape 键监听器，统一处理确认弹窗和抽屉关闭行为。
- 清理冗余前端代码与无效 CSS 选择器。

### 修复

- 修复操作栏遮挡新建或编辑会话弹窗的问题。
- 修复从默认标签栏进入会话时首页背景内容仍透出的问题。
- 统一深色模式下终端与全局表面的背景透明度。
- 修复浅色模式背景不生效或未贯穿全局表面的问题。
- 修复字体颜色设置不生效，以及选色控件与标签未对齐的问题。

---

## [0.1.0] · 2026-08-13

### 新增

- 提供多会话 SSH 终端，支持自动重连与终端尺寸同步。
- 支持密码和私钥认证，并可选校验 SSH 主机指纹。
- 支持本地保存和管理连接配置。
- 提供 SFTP 上传、下载、远程目录浏览及同名文件冲突处理。
- 实时显示主机名、SSH 延迟、CPU、内存与连接时长等健康信息。
- 提供深色与浅色主题。
- 支持 Windows 便携版与飞牛 fnOS 原生应用（FPK，兼容 x86 与 ARM，不依赖 Docker）打包。

---

<div align="center">

[查看所有版本](https://github.com/yingxin20000303/pig/releases) · [提交问题](https://github.com/yingxin20000303/pig/issues)

</div>
